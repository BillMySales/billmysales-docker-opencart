# OpenCart stack: Caddy + OpenCart (PHP-FPM, own image) + MariaDB, with a
# setup job, cron and optional backups and Mailpit.
#
# Quick start (development):
#   cp .env.dev.example .env
#   docker compose up -d
#   docker compose logs -f setup   # wait for "==> Done"
#
# Production: start from .env.prod.example instead. See README.md.
#
# Required variables have no default: compose refuses to start without them.

name: ${COMPOSE_PROJECT_NAME:-opencart}

x-logging: &logging
  driver: json-file
  options:
    max-size: ${LOG_MAX_SIZE:-10m}
    max-file: "${LOG_MAX_FILE:-3}"

# OpenCart image, built from image/Dockerfile (php:<ver>-fpm-alpine + the
# official release). Used by the app, setup and cron services.
x-oc-image: &oc-image
  image: ${OC_IMAGE:-opencart-fpm}:${OC_VERSION:-4.1.0.4}-php${PHP_VERSION:-8.5}
  build:
    context: ./image
    args:
      PHP_VERSION: ${PHP_VERSION:-8.5}
      OC_VERSION: ${OC_VERSION:-4.1.0.4}
      # Checksum of the default version; set it for another version (or empty to skip).
      OC_SHA256: ${OC_SHA256-cdef537f5a0a7e1b4ddbe16f589dd2e80b10150a09bfbe93ce76ef2d5c3218c0}

# Environment shared by PHP-FPM, setup and cron.
x-oc-env: &oc-env
  OC_URL: ${OC_URL:?Set OC_URL in .env (e.g. https://shop.example.com)}
  PHP_MEMORY_LIMIT: ${PHP_MEMORY_LIMIT:-256M}
  PHP_DISPLAY_ERRORS: ${PHP_DISPLAY_ERRORS:-Off}
  PHP_MAX_EXECUTION_TIME: ${PHP_MAX_EXECUTION_TIME:-300}
  PHP_TIMEZONE: ${PHP_TIMEZONE:-America/Santiago}
  UPLOAD_MAX_SIZE: ${UPLOAD_MAX_SIZE:-64M}

# Files mounted in PHP-FPM, setup and cron.
x-oc-volumes: &oc-volumes
  - code:/var/www/html
  - storage:/var/www/storage
  - ./config/php/php.ini:/usr/local/etc/php/conf.d/zz-stack.ini:ro
  - ./scripts:/usr/local/share/stack/scripts:ro

services:
  db:
    image: mariadb:${MARIADB_VERSION:-12.3}
    restart: unless-stopped
    logging: *logging
    environment:
      MARIADB_DATABASE: ${DB_NAME:-opencart}
      MARIADB_USER: ${DB_USER:-opencart}
      MARIADB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:?Set DB_ROOT_PASSWORD in .env}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: ${DB_MEMORY_LIMIT:-1g}

  # PHP-FPM with OpenCart. Not published; Caddy talks to it on port 9000.
  opencart:
    <<: *oc-image
    restart: unless-stopped
    logging: *logging
    depends_on:
      db:
        condition: service_healthy
    environment:
      <<: *oc-env
      PHP_FPM_MAX_CHILDREN: ${PHP_FPM_MAX_CHILDREN:-10}
      PHP_FPM_START_SERVERS: ${PHP_FPM_START_SERVERS:-2}
      PHP_FPM_MIN_SPARE_SERVERS: ${PHP_FPM_MIN_SPARE_SERVERS:-1}
      PHP_FPM_MAX_SPARE_SERVERS: ${PHP_FPM_MAX_SPARE_SERVERS:-4}
      PHP_FPM_MAX_REQUESTS: ${PHP_FPM_MAX_REQUESTS:-500}
    # Lets OpenCart reach services on the host (e.g. an app running locally)
    # at http://host.docker.internal:<port>, also on Linux.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - code:/var/www/html
      - storage:/var/www/storage
      - ./config/php/php.ini:/usr/local/etc/php/conf.d/zz-stack.ini:ro
      - ./config/php/fpm-pool.conf:/usr/local/etc/php-fpm.d/zz-stack.conf:ro
    healthcheck:
      test: ["CMD", "php", "-r", "exit(@fsockopen('127.0.0.1', 9000) ? 0 : 1);"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: ${OC_MEMORY_LIMIT:-1g}

  caddy:
    image: caddy:${CADDY_VERSION:-2.11}-alpine
    restart: unless-stopped
    logging: *logging
    depends_on:
      opencart:
        condition: service_healthy
    environment:
      SITE_ADDRESS: ${SITE_ADDRESS:-:80}
      UPLOAD_MAX_SIZE: ${UPLOAD_MAX_SIZE:-64M}
      OC_ADMIN_DIR: ${OC_ADMIN_DIR:?Set OC_ADMIN_DIR in .env (back office directory name)}
    ports:
      - "${HTTP_BIND:-127.0.0.1}:${HTTP_PORT:-80}:80"
      - "${HTTP_BIND:-127.0.0.1}:${HTTPS_PORT:-443}:443"
      - "${HTTP_BIND:-127.0.0.1}:${HTTPS_PORT:-443}:443/udp"
    volumes:
      # Read-only, code only: Caddy serves static files and forwards PHP to FPM.
      - code:/var/www/html:ro
      - ./config/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      # TLS certificates and keys (keep them: ACME rate limits).
      - caddy_data:/data
      - caddy_config:/config
    healthcheck:
      # Internal :8080 site: any 2xx/3xx means Caddy + PHP + OpenCart answer
      # (a redirect to the installer before setup has run).
      test: ["CMD-SHELL", "curl -so /dev/null -w '%{http_code}' http://localhost:8080/index.php | grep -qE '^[23]'"]
      interval: 5s
      timeout: 10s
      retries: 60
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: ${CADDY_MEMORY_LIMIT:-256m}

  # One-shot job: copies OpenCart to the volume, installs it and applies the
  # environment. Safe to repeat; runs on every `up`. Starts as root to fix
  # ownership, runs PHP as www-data (82).
  setup:
    <<: *oc-image
    user: root
    logging: *logging
    depends_on:
      opencart:
        condition: service_healthy
    environment:
      <<: *oc-env
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-opencart}
      DB_USER: ${DB_USER:-opencart}
      DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
      OC_ADMIN_DIR: ${OC_ADMIN_DIR:?Set OC_ADMIN_DIR in .env (back office directory name)}
      OC_ADMIN_USER: ${OC_ADMIN_USER:-admin}
      OC_ADMIN_PASSWORD: ${OC_ADMIN_PASSWORD:?Set OC_ADMIN_PASSWORD in .env}
      OC_ADMIN_EMAIL: ${OC_ADMIN_EMAIL:?Set OC_ADMIN_EMAIL in .env}
      OC_DEBUG: ${OC_DEBUG:-0}
      OC_STORE_NAME: ${OC_STORE_NAME:-OpenCart}
      OC_COUNTRY: ${OC_COUNTRY:-CL}
      OC_ZONE: ${OC_ZONE:-RM}
      OC_CURRENCY: ${OC_CURRENCY:-CLP}
      OC_CURRENCY_TITLE: ${OC_CURRENCY_TITLE:-Peso chileno}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_SECURE: ${SMTP_SECURE:-tls}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-}
    volumes: *oc-volumes
    entrypoint: ["sh", "/usr/local/share/stack/scripts/setup.sh"]
    restart: "no"

  # Runs OpenCart's cron.php every CRON_INTERVAL seconds.
  cron:
    <<: *oc-image
    user: www-data
    restart: unless-stopped
    logging: *logging
    depends_on:
      setup:
        condition: service_completed_successfully
    environment:
      <<: *oc-env
      CRON_INTERVAL: ${CRON_INTERVAL:-300}
    volumes: *oc-volumes
    entrypoint: ["sh", "/usr/local/share/stack/scripts/cron.sh"]
    healthcheck:
      test: ["CMD", "sh", "/usr/local/share/stack/scripts/cron.sh", "health"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: ${CRON_MEMORY_LIMIT:-512m}

  # Optional (profile "backup"): DB dump + store files every
  # BACKUP_INTERVAL_HOURS, kept BACKUP_KEEP_DAYS days in the `backups` volume
  # (overrides/local-dirs.yaml puts them in a local directory instead).
  backup:
    image: mariadb:${MARIADB_VERSION:-12.3}
    profiles: ["backup"]
    restart: unless-stopped
    logging: *logging
    depends_on:
      setup:
        condition: service_completed_successfully
    environment:
      DB_HOST: db
      DB_NAME: ${DB_NAME:-opencart}
      DB_USER: ${DB_USER:-opencart}
      DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
      BACKUP_INTERVAL_HOURS: ${BACKUP_INTERVAL_HOURS:-24}
      BACKUP_KEEP_DAYS: ${BACKUP_KEEP_DAYS:-7}
    volumes:
      # Read-write so `backup.sh restore` can put the files back.
      - code:/var/www/html
      - storage:/var/www/storage
      - backups:/backups
      - ./scripts/backup.sh:/usr/local/bin/backup.sh:ro
    entrypoint: ["bash", "/usr/local/bin/backup.sh"]
    healthcheck:
      test: ["CMD", "bash", "/usr/local/bin/backup.sh", "health"]
      interval: 5m
      timeout: 10s
      retries: 3
      start_period: 5m
    deploy:
      resources:
        limits:
          memory: ${BACKUP_MEMORY_LIMIT:-512m}

  # Optional (profile "mailpit", development): catches all outgoing mail.
  # Set SMTP_HOST=mailpit and SMTP_PORT=1025; web UI on MAILPIT_PORT.
  mailpit:
    image: axllent/mailpit:${MAILPIT_VERSION:-v1.31}
    profiles: ["mailpit"]
    restart: unless-stopped
    logging: *logging
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
    ports:
      - "${HTTP_BIND:-127.0.0.1}:${MAILPIT_PORT:-8025}:8025"

volumes:
  db_data:
  code:
  storage:
  caddy_data:
  caddy_config:
  backups:
